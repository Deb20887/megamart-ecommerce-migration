apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: megamart
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
