apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: megamart
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
